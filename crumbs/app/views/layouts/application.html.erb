<!DOCTYPE html>
<html>
<head>
  <title>Crumbs</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div>
      <nav>
        <li class="navbar">
          <ul class="navbutton">About</ul>
          <ul class="navbutton">Projects</ul>
          <ul class="navbutton">Students</ul>
          <ul class="navbutton">Organizations</ul>
          <ul class="navbutton">Contact</ul>
        </li>
        <div class="login">
          <% if current_user && current_user != nil %>
            <p>Hello <%= link_to current_user.first_name,
        student_show_path(current_user.name, current_user.id) %> </p>
            <p><%= link_to 'Log Out', logout_path, method: :delete %></p>
          <% else %>
            <p><%= link_to 'Log In with Facebook', '/auth/facebook' %></p>
          <% end %>
        </div>
      </nav>
    <header>
      <h1 class="title">Website Title</h1>
      <h3>Connecting Talent With Possibilities</h3>
    </header>
    <body>
      <%= yield %>
    </body>
  </div>
</body>
</html>
